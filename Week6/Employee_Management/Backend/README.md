# Employee Management - Backend

An Express.js REST API server using Mongoose to interface with MongoDB.

## Development Commands
- **Start Server**: `npm start`

## Environment Variables
- `DB_URL`: MongoDB Atlas connection URI string.
- `PORT`: Port number to listen on (defaults to `3000`).
- `CORS_ORIGIN`: Allowed origins for browser fetch permissions (defaults to `*` if not defined).
